import RPi.GPIO as gpio
import time

gpio.setmode(gpio.BOARD)
'''
pin = 8
gpio.setup(pin, gpio.OUT)
gpio.output(pin,True)
time.sleep(2)



pin = 10
gpio.setup(pin, gpio.OUT)
gpio.output(pin,True)
time.sleep(2)

pin = 11
gpio.setup(pin, gpio.OUT)
gpio.output(pin,True)
time.sleep(2)

pin = 12
gpio.setup(pin, gpio.OUT)
gpio.output(pin,True)
time.sleep(2)

pin = 13
gpio.setup(pin, gpio.OUT)
gpio.output(pin,True)
time.sleep(2)




pin = 15
gpio.setup(pin, gpio.OUT)
gpio.output(pin,True)
time.sleep(2)


pin = 16
gpio.setup(pin, gpio.OUT)
gpio.output(pin,True)
time.sleep(2)


'''

pin = 18
gpio.setup(pin, gpio.OUT)
gpio.output(pin,True)
time.sleep(2)


pin = 19
gpio.setup(pin, gpio.OUT)
gpio.output(pin,True)
time.sleep(2)




pin = 21
gpio.setup(pin, gpio.OUT)
gpio.output(pin,True)
time.sleep(2)






gpio.cleanup()
